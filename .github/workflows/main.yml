- name: Generate Environment Config
        run: |
          # Creates a brand new file called env.js with your secret URL
          echo "window.VOICEPRESS_CONFIG = { API_URL: '${{ secrets.RENDER_BACKEND_URL }}' };" > env.js
          echo "Config file created successfully."
